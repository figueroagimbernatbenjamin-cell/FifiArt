import React from "react";
import ReactDOM from "react-dom/client";
import GaleriaArte from "../GaleriaArte.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GaleriaArte />
  </React.StrictMode>
);
